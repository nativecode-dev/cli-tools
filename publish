#!/bin/bash

MODE=$1
LERNA=./node_modules/.bin/lerna

case $MODE in
"canary")
    ${LERNA} publish --canary
    ;;
"release")
    ${LERNA} publish from-package --create-release gitlab --dist-tag latest
    ;;
"prerelease")
    ${LERNA} publish from-package --dist-tag next --preid next
    ;;
esac
