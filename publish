#!/bin/bash

set -e

MODE=$1
LERNA=./node_modules/.bin/lerna

case $MODE in
"canary")
    ${LERNA} publish --canary
    ;;
"release")
    ${LERNA} publish  --conventional-commits --create-release gitlab --dist-tag latest
    ;;
"prerelease")
    ${LERNA} publish  --conventional-commits --dist-tag next --preid next
    ;;
esac
